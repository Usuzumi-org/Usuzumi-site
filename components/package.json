{
  "private": true,
  "type": "module",
  "workspaces": [
    "runtime"
  ],
  "scripts": {
    "build": "node ../scripts/build.mjs --components-only"
  },
  "dependencies": {
    "usuzumi": "1.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
