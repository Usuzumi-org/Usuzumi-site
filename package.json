{
  "name": "usuzumi-site",
  "private": true,
  "type": "module",
  "description": "Documentation and component demos for Usuzumi.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check:build": "node scripts/build.mjs --check",
    "validate": "npm run check:build && node scripts/validate.mjs"
  },
  "dependencies": {
    "usuzumi": "file:.."
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
