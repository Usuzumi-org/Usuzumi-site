{
  "name": "usuzumi-site",
  "private": true,
  "type": "module",
  "description": "Documentation and component demos for Usuzumi.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check:build": "node scripts/build.mjs --check",
    "validate": "npm run check:build && node scripts/validate.mjs && node scripts/browser-smoke.mjs"
  },
  "dependencies": {
    "usuzumi": "1.2.0"
  }
}
